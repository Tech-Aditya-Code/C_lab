#include <stdio.h>

int main()
{
    // WAP to get percentage of 5 sub

    float m1 = 95; // maths marks
    float m2 = 77;  // physics marks
    float m3 = 92;  // chem marks
    float m4 = 90;  // english marks
    float m5 = 86;  // economics marks
    float p = (m1 + m2 + m3 + m4 + m5) / 5; 
    printf("Percentage of 5 subjects: %f",p);

    return 0;
}